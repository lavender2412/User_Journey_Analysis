# FastAPI runtime dependencies — used by Elastic Beanstalk
# Keep this file lean: only what app.py and feature_utils.py need at runtime.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
httpx>=0.27.0        # required by FastAPI's TestClient (starlette dependency)
