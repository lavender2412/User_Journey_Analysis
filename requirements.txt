pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
flask>=3.0
gunicorn>=21.0
jupyter>=1.0
ipykernel>=6.0
# PySpark ETL layer (spark_pipeline.py) — not deployed to Elastic Beanstalk
# On EMR/Databricks, PySpark is pre-installed on the cluster; install locally for dev.
pyspark>=3.5
pyarrow>=14.0   # Arrow-accelerated Pandas UDFs (mapInPandas)
